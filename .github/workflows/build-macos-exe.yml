name: Build macOS EXE (PyInstaller)

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pyinstaller

      - name: Inject Gemini key (build-time)
        shell: bash
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          python - <<'PY'
          import os
          from pathlib import Path
          key = (os.environ.get("GEMINI_API_KEY") or "").strip()
          if not key:
            raise SystemExit("Missing GitHub Actions secret GEMINI_API_KEY (Settings → Secrets and variables → Actions).")
          Path("scheduleanalytics_secrets.py").write_text(f'EMBEDDED_GEMINI_API_KEY = {key!r}\n', encoding="utf-8")
          PY
          python -c "import scheduleanalytics_secrets as s, sys; k=getattr(s,'EMBEDDED_GEMINI_API_KEY',''); print('Injected key length:', len(k)); sys.exit(0 if k else 1)"

      - name: Inject build info (build-time)
        shell: bash
        run: |
          python - <<'PY'
          import os
          import datetime
          from pathlib import Path
          sha = (os.environ.get("GITHUB_SHA") or "").strip()
          ts = datetime.datetime.utcnow().replace(microsecond=0).isoformat() + "Z"
          Path("scheduleanalytics_build.py").write_text(f"BUILD_SHA = {sha!r}\nBUILD_TIME_UTC = {ts!r}\n", encoding="utf-8")
          PY

      - name: Build (native arch)
        run: |
          pyinstaller --noconfirm --clean --onefile --windowed --hidden-import scheduleanalytics_secrets --hidden-import scheduleanalytics_build --name ScheduleAnalytics main.py
          chmod +x dist/ScheduleAnalytics
          file dist/ScheduleAnalytics

      - name: SHA256
        run: |
          shasum -a 256 dist/ScheduleAnalytics

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ScheduleAnalytics-macos
          path: dist/ScheduleAnalytics
