name: Build macOS EXE (PyInstaller)

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pyinstaller

      - name: Build (native arch)
        run: |
          pyinstaller --noconfirm --clean --onefile --windowed --name ScheduleAnalytics main.py
          chmod +x dist/ScheduleAnalytics
          file dist/ScheduleAnalytics

      - name: Build (x86_64 via Rosetta)
        run: |
          mkdir -p dist_x86
          arch -x86_64 python -m pip install --upgrade pip
          arch -x86_64 python -m pip install -r requirements.txt
          arch -x86_64 python -m pip install pyinstaller
          arch -x86_64 pyinstaller --noconfirm --clean --onefile --windowed --name ScheduleAnalytics-x86_64 main.py
          chmod +x dist/ScheduleAnalytics-x86_64
          file dist/ScheduleAnalytics-x86_64

      - name: SHA256
        run: |
          shasum -a 256 dist/ScheduleAnalytics
          shasum -a 256 dist/ScheduleAnalytics-x86_64

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ScheduleAnalytics-macos
          path: dist/ScheduleAnalytics

      - name: Upload artifact (x86_64)
        uses: actions/upload-artifact@v4
        with:
          name: ScheduleAnalytics-macos-x86_64
          path: dist/ScheduleAnalytics-x86_64
